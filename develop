список файлов
    + получить последний по дате
    + получить за неделю (или менее)
        + получить дату из строки


сравниваем строки:
    получаем строки первого, второго
    получаем только новые строки

    заполняем новые строки всеми данными
        сравниваем названия
        сравниваем вектора
